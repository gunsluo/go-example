# README

```
docker plugin install grafana/loki-docker-driver:latest --alias loki --grant-all-permissions
docker plugin install grafana/loki-docker-driver:latest-bridge --alias loki --grant-all-permissions
```

